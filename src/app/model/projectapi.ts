export class Projectapi{
  name: string;

  state: string;

  DL
  client;

  constructor(name: string, state: string, deadline,cl) {
    this.name = name;
    this.state = state;
    this.DL = deadline;
    this.client=cl;
  }
}
